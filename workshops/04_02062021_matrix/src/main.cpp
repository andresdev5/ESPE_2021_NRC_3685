#include <iostream>
#include "matrix.h"
#include "matrix_utils.h"

int main(int argc, char **argv) {
    int size = 10;

    Matrix<int> matrixA(size);
    Matrix<int> matrixB(size);

    return 0;
}
